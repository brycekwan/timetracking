<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New work order</h1>

  <%= render partial: "form", locals: { work_order: @work_order, projects: @projects, users: @users } %>

  <%= link_to 'Back to work orders', work_orders_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
